name: Quadran-Lock Guards
on:
  pull_request:
    paths-ignore: [ 'node_modules/**' ]
  push:
    branches: [ main, merge-review, fix/**, chore/**, feat/** ]
jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci
      - name: Check for banned tokens (Dumb Ass Protocol)
        run: npm run ci:ban-terms
      - name: Dumb Ass Protocol Enforcement
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const { context } = require('@actions/github');
            if (context.payload.pull_request) {
              const issue_number = context.payload.pull_request.number;
              await github.rest.issues.createComment({
                ...context.repo,
                issue_number,
                body: `🚨 **DUMB ASS PROTOCOL ACTIVATED** 🚨\n\nAuthor: @dumbass (Sonnet)\nViolation: Quadran-Lock naming rollback attempt.\n\n👉 Pull head from anus and get smarter.`
              });
            }
      - run: npm run ci:structure-guards
      - run: npm run test:quadran-lock